{
	"scripts": {
		"init": "npm install && tns install && cd src && npm link typescript && npm link gulp && npm link gulp-typescript && npm install && gulp clean",
		"src": "cd src && gulp",
		"clean": "cd src && gulp clean",
		"p": "tns livesync android --watch",
		"i": "tns livesync ios --watch",
		"lodash": "lodash include=isEmpty,isUndefined,isNull,keysIn,functionsIn,difference,merge,debounce,throttle,pick,omit,isBoolean,isString,capitalize,remove,isFinite,intersectionBy"
	},
	"nativescript": {
		"id": "com.roblav96.playground",
		"tns-android": {
			"version": "next"
		},
		"tns-ios": {
			"version": "next"
		}
	},
	"dependencies": {
		"ansicolors": "latest",
		"ansistyles": "latest",
		"eventemitter3": "latest",
		"nativescript-playground-plugin": "file:plugin",
		"tns-core-modules": "next"
	},
	"devDependencies": {
		"tns-platform-declarations": "next",
		"babel-traverse": "latest",
		"babel-types": "latest",
		"babylon": "latest",
		"lazy": "latest",
		"zone.js": "latest"
	}
}

